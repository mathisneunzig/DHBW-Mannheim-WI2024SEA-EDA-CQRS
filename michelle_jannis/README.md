# Das schwarze Brett

RabbitMQ starten: `podman compose up -d` bzw. `docker compose up -d`
Subscriber und Publisher jeweils im Ordner starten: `go run main.go`

Ports für die Webseiten: 8080 (Publisher) und 8081 (Subscriber) 